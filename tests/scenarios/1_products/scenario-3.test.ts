describe("Cenário 3 - Obtenção de produtos por ID (GET /api/v1/products/{id})", () => {
    test("3.1 - Listar produto por ID através de requisição válida", () => {});

    test("3.2 - Retornar erro de formato para parâmetro 'id' do tipo string não numérica", () => {});

    test("3.3 - Retornar erro para parâmetro 'id' não cadastrado", () => {});
});