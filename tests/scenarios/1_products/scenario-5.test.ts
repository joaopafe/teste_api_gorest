describe("Cenário 5 - Remoção de produtos (DELETE /api/v1/products/{id})", () => {
    test("5.1 - Remover produto por ID através de requisição válida", () => {});

    test("5.2 - Retornar erro de formato para parâmetro 'id' do tipo string não numérica", () => {});

    test("5.3 - Retornar erro para parâmetro 'id' não cadastrado", () => {});
});