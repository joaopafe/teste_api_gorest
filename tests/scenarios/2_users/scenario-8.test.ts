describe("Cenário 8 - Obtenção de usuários por ID (POST /api/v1/users/{id})", () => {
    test("8.1 - Listar usuário por ID através de requisição válida", () => {});
    
    test("8.2 - Retornar erro de formato para parâmetro 'id' do tipo string não numérica", () => {});

    test("8.3 - Retornar erro para parâmetro 'id' não cadastrado", () => {});
});