describe("Cenário 10 - Remoção de usuários (DELETE /api/v1/users/{id})", () => {
    test("5.1 - Remover usuário por ID através de requisição válida", () => {});

    test("5.2 - Retornar erro para parâmetro 'id' do tipo string não numérica", () => {});

    test("5.3 - Retornar erro para parâmetro 'id' não cadastrado", () => {});
});