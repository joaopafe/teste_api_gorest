describe("Cenário 15 - Remoção de categorias (DELETE /api/v1/categories/{id})", () => {
    test("15.1 - Remover categoria por ID através de requisição válida", () => {});

    test("15.2 - Retornar erro para parâmetro 'id' do tipo string não numérica", () => {});

    test("15.3 - Retornar erro para parâmetro 'id' não cadastrado", () => {});
});