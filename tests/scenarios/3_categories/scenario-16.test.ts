describe("Cenário 16 - Obtenção de produtos por categoria (GET /api/v1/categories/{id}/products)", () => {
    test("16.1 - Listar produtos por categoria através de requisição válida", () => {});
    
    test("16.2 - Retornar lista vazia para requisição com parâmetros 'limit' e 'offset' acima do valor limite", () => {});
    
    test("16.3 - Retornar lista vazia para requisição com parâmetro 'id' não cadastrado", () => {});
    
    test("16.4 - Retornar erro de formato para requisição com parâmetro 'limit' do tipo string", () => {});

    test("16.5 - Retornar erro de formato para requisição com parâmetro 'offset' do tipo string", () => {});

    test("16.6 - Retornar erro de formato para requisição com parâmetro 'limit' e 'offset' do tipo string", () => {});
});