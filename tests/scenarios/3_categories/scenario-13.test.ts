describe("Cenário 13 - Obtenção de categorias por ID (GET /api/v1/categories/{id})", () => {
    test("13.1 - Listar categoria por ID através de requisição válida", () => {});
    
    test("13.2 - Retornar erro de formato para parâmetro 'id' do tipo string não numérica", () => {});

    test("13.3 - Retornar erro para parâmetro 'id' não cadastrado", () => {});
});